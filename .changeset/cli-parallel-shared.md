---
"thermoworks": minor
---

Parallelize per-device channel fetches in copilot status for faster output. Use shared alarm, credential, and config utilities from SDK. Fix static imports for --version flag and flaky time-dependent test.
